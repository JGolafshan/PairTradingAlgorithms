@echo off

REM Move one directory up to the project root
cd ..

cd ..

REM Run the Python script using Python interpreter
pip install -r requirements.txt

REM Pause to keep the window open after execution
pause